import React from "react";

import { PreviewProps } from "../../common/interfaces";
import Layout from "../../components/Layout";

const HeaderSettingsPreview: React.FC<PreviewProps> = ({}) => <Layout title="头部设置" />;

export default HeaderSettingsPreview;

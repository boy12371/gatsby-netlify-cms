import React from "react";

import Logo from "../../components/Logo";
import { PreviewProps } from "../../common/interfaces";

const LogoSettingsPreview: React.FC<PreviewProps> = ({}) => <Logo />;

export default LogoSettingsPreview;
